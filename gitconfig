[core]
	excludesfile = ~/.gitignore
	editor = "nvim"
  pager = delta
[interactive]
  diffFilter = delta --color-only
[delta]
  navigate = true
  dark = true
  line-numbers = true
[merge]
  conflictStyle= zdiff3
[includeIf "gitdir:~/"]
  path = .gitconfig-personal
[includeIf "gitdir:~/projects/work/"]
	path = .gitconfig-work
[credential]
	helper = osxkeychain
	helper =
	helper = /usr/local/share/gcm-core/git-credential-manager
[github]
	user = ulissesalmeida
[push]
	default = simple
[pull]
	default = rebase
	rebase = true
[fetch]
	prune = true
[alias]
	# breakroom = !git config user.email ulisses.herrera@breakroom.cc
	save = !git add -A && git commit -m wip
	undo = reset --soft HEAD^
	patch = add -p
[color]
  ui = true
  interactive = true
  status = true
  branch = true
  diff = true
[apply]
  whitespace = fix
[gpg]
	program = gpg
[commit]
	gpgsign = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[credential "https://github.com"]
	helper =
	helper = !/opt/homebrew/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !/opt/homebrew/bin/gh auth git-credential
[credential "https://dev.azure.com"]
	useHttpPath = true
